name: template_flutter_provider
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.6
  auto_injector: ^2.0.3
  intl: ^0.18.1
  http: ^1.2.1
  uno: ^1.1.9
  mockito: ^5.4.4
  dartz: ^0.10.1
  google_fonts: ^6.2.1
  dio: ^5.4.2+1
  provider: ^6.1.2
  go_router: ^13.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  

flutter:
  uses-material-design: true

flutter_intl:
  enabled: true